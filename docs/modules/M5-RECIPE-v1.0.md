# M5-RECIPE v1.0: AI 레시피 — 단일 경로 시스템

> **버전**: 1.0 (초기 구현)
> **상태**: 운영 중 — v2 미전환 레시피에 적용
> **인덱스**: [M5-RECIPE.md](./M5-RECIPE.md)

---

## 1. 개요

멀티 AI 워크플로우 가이드. 여러 AI 도구를 조합해 복잡한 작업을 완성하는 방법을 단계별로 안내한다.

**핵심 구조**: 하나의 레시피 = 하나의 고정 경로 (Step 1 → 2 → 3 → 결과물)

**데이터 소스**: `data/recipes.ts` (로컬 TypeScript 파일, 100개+ 레시피)

---

## 2. 데이터 구조

### 2.1 타입 정의

```typescript
type RecipeDifficulty = 'easy' | 'medium' | 'hard';

type RecipeCategory =
  | 'music' | 'video' | 'image' | 'marketing' | 'presentation'
  | 'blog' | 'social' | 'education' | 'ecommerce' | 'podcast'
  | 'brand' | 'comic' | '3d'
  | 'writing' | 'design' | 'audio' | 'coding' | 'business' | 'data' | 'personal';

interface RecipeStep {
  step: number;
  title: string;
  tool_slug: string;        // AIPICK 도구 slug (링크 연결)
  tool_name: string;         // 표시용 도구명
  alt_tools?: string[];      // 대안 도구 slug 목록
  action: string;            // 이 단계에서 하는 일
  prompt_example?: string;   // 프롬프트 예시
  tip?: string;              // 핵심 팁
}

interface AIRecipe {
  slug: string;
  title: string;
  subtitle: string;
  category: RecipeCategory;
  difficulty: RecipeDifficulty;
  estimated_time: string;    // "30분", "1~2시간" 등
  tool_count: number;
  steps: RecipeStep[];
  result_description: string;
  tags: string[];
  icon: string;              // Lucide icon name — DynamicIcon ICON_MAP에 등록 필수
  color: string;             // Tailwind gradient class
}
```

### 2.2 난이도 3단계

| 난이도 | 라벨 | 스타일 |
|--------|------|--------|
| `easy` | 쉬움 | `bg-emerald-100 text-emerald-700` |
| `medium` | 보통 | `bg-amber-100 text-amber-700` |
| `hard` | 어려움 | `bg-red-100 text-red-700` |

---

## 3. 레시피 카테고리 (20종)

| 카테고리 | 라벨 | 아이콘 |
|---------|------|--------|
| music | 음악 제작 | Music |
| audio | 오디오 | Headphones |
| video | 영상 제작 | Video |
| image | 이미지/캐릭터 | Image |
| design | 디자인 | Palette |
| writing | 글쓰기 | PenTool |
| blog | 블로그/아티클 | FileText |
| marketing | 마케팅 | Megaphone |
| presentation | 발표자료 | Presentation |
| social | 숏폼/SNS | Smartphone |
| education | 교육 콘텐츠 | GraduationCap |
| ecommerce | 상품 사진 | ShoppingBag |
| podcast | 팟캐스트 | Mic |
| brand | 브랜드/로고 | Sparkles |
| business | 비즈니스 | Briefcase |
| coding | 코딩 | Code |
| data | 데이터 | BarChart |
| personal | 개인/라이프 | User |
| comic | 만화/웹툰 | BookOpen |
| 3d | 3D 모델 | Box |

---

## 4. 페이지 구성

### 4.1 목록 페이지 (`/recipes`)

```
┌─────────────────────────────────────────┐
│ 📖 AI 레시피                              │
│ 여러 AI를 조합해서 결과물 만드는 가이드     │
├─────────────────────────────────────────┤
│ [카테고리 필터 탭]                         │
├─────────────────────────────────────────┤
│ 총 N개 레시피                             │
│                                          │
│ [레시피 카드] — 가로형 레이아웃             │
│  왼쪽: 아이콘 + 제목 + 난이도 + 도구수     │
│  오른쪽: 단계 가로 나열 (Step 1 → 2 → 3)  │
│  하단: 완성 결과물 + CTA                  │
└─────────────────────────────────────────┘
```

- 정렬: 난이도 오름차순 (쉬움→보통→어려움), 같은 난이도 내 tool_count 적은 순

### 4.2 상세 페이지 (`/recipes/[slug]`)

```
┌─────────────────────────────────────────┐
│ ← AI 레시피 목록                          │
│                                          │
│ [카테고리 배지] [난이도 배지]              │
│ 레시피 제목                               │
│ 부제목                                    │
│ AI N개 사용 · 소요시간 · 결과물 설명       │
├─────────────────────────────────────────┤
│ 이 레시피에 사용되는 AI                    │
│ [도구 1] [도구 2] [도구 3]                │
├─────────────────────────────────────────┤
│ 📖 단계별 가이드                          │
│ ① Step 1 — [도구명] 대안: xxx            │
│ │  설명, 프롬프트 예시, 팁                 │
│ │                                         │
│ ② Step 2 — [도구명]                      │
│ │  설명, 프롬프트 예시, 팁                 │
│ │                                         │
│ ③ Step 3 — [도구명]                      │
│    설명, 프롬프트 예시, 팁                 │
├─────────────────────────────────────────┤
│ 완성 결과물 + 태그                        │
├─────────────────────────────────────────┤
│ 커뮤니티 (관련 글)                         │
├─────────────────────────────────────────┤
│ 관련 레시피 캐러셀                         │
└─────────────────────────────────────────┘
```

---

## 5. 핵심 규칙

### 아이콘 등록 필수
- 레시피의 `icon` 필드에 사용하는 Lucide 아이콘은 반드시 `components/ui/DynamicIcon.tsx`의 `ICON_MAP`에 등록
- 미등록 시 빌드 에러 또는 아이콘 미표시

### 데이터 작성 규칙
- `tool_slug`는 AIPICK DB의 도구 slug와 일치해야 함 (링크 연결)
- `tool_count`는 고유 도구 수와 일치
- `steps`는 1부터 순차 번호
- `color`는 유효한 Tailwind gradient 클래스

### alt_tools 규칙
- 대안 도구의 slug만 배열로 나열
- 대안 도구의 사용법, 장단점은 설명하지 않음 (v1 한계)

---

## 6. 컴포넌트

| 컴포넌트 | 파일 | 역할 |
|---------|------|------|
| RecipeCard | `components/recipe/RecipeCard.tsx` | 목록 카드 (가로형) |
| RecipeStepCard | `components/recipe/RecipeStepCard.tsx` | 상세 단계 표시 |
| RecipeCategoryFilter | `components/recipe/RecipeCategoryFilter.tsx` | 카테고리 필터 |
| RelatedRecipeCarousel | `components/recipe/RelatedRecipeCarousel.tsx` | 관련 레시피 |
| RecipeCommunitySection | `components/recipe/RecipeCommunitySection.tsx` | 커뮤니티 연계 |
| RecipeCarousel | `components/home/RecipeCarousel.tsx` | 홈 캐러셀 |

---

## 7. 커뮤니티 연계

레시피 페이지 하단에 관련 커뮤니티 글을 표시:
- 레시피의 도구 slug + 태그를 OR 필터로 커뮤니티 글 조회
- 커뮤니티 글 작성 시 레시피 도구 slug가 자동 태그로 추가

---

## 8. v1의 한계점 (→ v2에서 해결)

| 한계 | 설명 |
|------|------|
| **단일 경로** | 같은 결과물을 만드는 다른 방법 제시 불가 |
| **alt_tools 피상적** | 대안 도구를 이름만 나열, 왜/어떻게 쓰는지 안내 없음 |
| **난이도 고정** | 레시피 전체에 하나의 난이도만 — 초보/고급 구분 불가 |
| **장단점 없음** | 방법 간 비교 불가 |
| **성장 경로 없음** | 쉬운 방법에서 어려운 방법으로 넘어가는 유도 없음 |

→ 해결: [M5-RECIPE-v2.0.md](./M5-RECIPE-v2.0.md)
